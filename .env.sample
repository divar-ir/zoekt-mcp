# Zoekt MCP Server Configuration

# Required: Zoekt API URL
ZOEKT_API_URL=https://zoekt.example.com/

# Optional: Basic Authentication (if your Zoekt instance requires auth)
# ZOEKT_API_LOGIN=your_username
# ZOEKT_API_PASSWORD=your_password

# Optional: SSL verification (default: true)
# Set to false for self-signed certificates
# ZOEKT_VERIFY_SSL=false

# Optional: Server ports
# MCP_SSE_PORT=8000
# MCP_STREAMABLE_HTTP_PORT=8080

# Optional: MCP transport mode (default: HTTP/SSE servers)
# Set to "stdio" to run on stdin/stdout — the mode used by MCP clients like
# Claude Code. Normally this is set by the MCP client in its server config
# (e.g. the `env` block of ~/.claude.json), NOT here. Uncommenting it here
# forces stdio mode for every local run, so leave it commented for HTTP/SSE.
# MCP_TRANSPORT=stdio
